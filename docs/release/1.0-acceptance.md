# SourceSlate 1.0 Acceptance Gate

A release candidate is eligible for `v1.0.0` only when the repository-controlled gates are implemented **and** every external validation gate has been observed passing.

Status used below:

- `[x]` — implemented and verified by repository code/tests/workflow execution.
- `[ ]` — still requires external validation or release execution before the tag is created.

Current verified integration candidate: `2f29773959e8bd186ad108fa7252362468a0c9ac`.

Observed CI evidence:

- PR readiness run `263`: all six PHP/OS matrix jobs green, Composer global-install smoke green, public HTTPS remote-build smoke green.
- Post-merge integration run `264`: the exact merged integration commit is green across the same complete matrix and smoke jobs.

## Installation and packaging

- [x] Composer metadata is configured for `phpwalter/sourceslate`, including the `sourceslate` binary.
- [x] `composer validate --strict` is part of CI/release validation.
- [x] PHP 8.3 and PHP 8.4 are validated on Linux, Windows, and macOS.
- [x] Composer archive generation is part of the release workflow.
- [x] The release workflow verifies that a `vX.Y.Z` tag matches `SourceSlate\Version::VERSION`.
- [x] A clean Composer home can install SourceSlate globally from the checked-out package and execute `sourceslate --version` successfully.
- [ ] Verify `composer global require phpwalter/sourceslate` from a clean machine after the package is published/available through Packagist.

## Local project build

- [x] Path-first invocation supports `sourceslate .`.
- [x] Local configuration and source discovery are deterministic.
- [x] Repeated rendering has full-tree reproducibility regression coverage.
- [x] `--check` performs a staging render and deterministic comparison.
- [x] Source include/exclude rules are represented by configuration and parser tests.
- [x] Unmanaged-output, symlink-output, and publication-recovery safeguards are implemented.
- [x] Forced publication-failure tests prove restoration of the previous documentation tree.
- [x] A failed automatic restoration preserves the backup path for manual recovery.
- [x] CI performs a clean documentation build followed immediately by deterministic `--check` on the final integration candidate.

## Remote project build

- [x] Persistent canonical repository cache is implemented.
- [x] Branch, tag, full SHA, and default-branch resolution have local bare-repository integration coverage.
- [x] Branch/tag ambiguity is rejected and explicit `--branch`/`--tag` disambiguation is covered.
- [x] Persistent cache reuse, offline reuse, refresh/update behavior, and dirty-worktree isolation are covered.
- [x] `--source-path` containment is enforced.
- [x] Recursive submodule checkout has deterministic local integration coverage and remains opt-in.
- [x] Abbreviated commit prefixes are validated and ambiguous prefixes return `SS-GIT-0109`.
- [x] A real public HTTPS repository build succeeds in CI against `https://github.com/phpwalter/SourceSlate.git`.
- [ ] Verify a private HTTPS repository using a real credential helper/token without secret leakage.
- [ ] Verify a private SSH repository using an SSH agent/key without secret leakage.

## Cache safety and recovery

- [x] Cache metadata schema and canonical repository identity are explicit.
- [x] Legacy unversioned/schema-0 metadata can be normalized to the current schema.
- [x] Metadata writes use controlled persistence and cache operations are lock-protected.
- [x] Repair stages and validates a replacement before swapping it into place.
- [x] Active cache entries are protected from repair, prune, and clear operations.
- [x] `cache:verify` checks metadata identity, bare repository presence, Git object integrity, origin identity, and cached worktrees.
- [x] `cache:clean` handles stale repair artifacts and released lock files.
- [x] `cache:list`, `cache:verify`, `cache:clean`, `cache:prune`, `cache:repair`, and `cache:clear` expose machine-readable output where maintenance automation needs it.
- [x] Interrupted publication and repair debris have recovery/cleanup behavior.
- [x] Lock/concurrency tests are green on Linux, Windows, and macOS in the final integration CI run.

## Parser and PHPDoc

- [x] Classes, interfaces, traits, enums, functions, methods, properties, constants, and enum cases are represented.
- [x] Nullable, union, and intersection native types are preserved.
- [x] Constructor-promoted properties, readonly state, parameter defaults, and native attributes are preserved.
- [x] Attribute names and positional/named argument expressions are preserved in the documentation model.
- [x] Standard PHPDoc handlers cover parameters, returns, throws, variables/properties/methods, templates, inheritance/deprecation/see-style metadata, and SourceSlate tags.
- [x] Unknown PHPDoc tags remain lossless through the fallback handler.
- [x] Parser/member regression fixtures cover modern PHP constructs.
- [x] Final PHP 8.3 and PHP 8.4 test suites are green on Linux, Windows, and macOS.

## Symbol resolution and links

- [x] Fully qualified symbols are indexed deterministically.
- [x] Same-namespace short names resolve when unambiguous.
- [x] Methods, properties, constants, enum cases, and functions are indexed.
- [x] Ambiguous short names return no guessed target.
- [x] Renderer URLs and symbol-index URLs use the same deterministic path conventions.
- [x] Internal inheritance/interface/trait relationships are linked only when resolution is unique.
- [x] Internal `@see` references become links only when resolution is unique.

## Generated site

- [x] Project, namespace, type, function, member, and source pages are generated.
- [x] Search index contains types and members.
- [x] Client-side search constructs DOM nodes with text properties rather than injecting generated values through `innerHTML`.
- [x] Keyboard search navigation and visible focus behavior are implemented.
- [x] Light/dark/system theme behavior, responsive layout, long-signature wrapping, and print rules are implemented.
- [x] Server-rendered source/PHPDoc text is escaped.
- [x] Renderer security regressions cover hostile generated content.
- [ ] Perform a final visual browser review of representative generated pages before tagging.

## Source-header updates

- [x] Local source updates are opt-in/configurable and idempotent.
- [x] Existing `@sourceslate` metadata is replaced rather than duplicated.
- [x] Dry-run reports changes without writing source.
- [x] Remote cached sources cannot be mutated.
- [x] Source replacement uses temporary/backup semantics.
- [x] Documentation output must remain inside the local project when source mutation is enabled.
- [x] External-output source mutation fails with `SS-SRC-0015` before a host filesystem path can be written into source.

## Diagnostics and operational support

- [x] `doctor --json` checks runtime, extensions, Git, cache/lock writability, repair debris, shallow cache-entry integrity, project/configuration/source paths, output writability, and output symlink safety.
- [x] `config:show` exposes the resolved configuration.
- [x] Stable diagnostic codes cover configuration, Git, cache, source, output, and documentation-staleness failures.
- [x] Troubleshooting, security, remote repository, CI, installation, and monorepo guides are present.
- [x] Benchmark tooling reports parse/render/total time and peak memory.
- [x] The diagnostics reference matches the final merged implementation.

## CI and release

- [x] CI is configured for Linux PHP 8.3 and 8.4.
- [x] CI is configured for Windows PHP 8.3 and 8.4.
- [x] CI is configured for macOS PHP 8.3 and 8.4.
- [x] CI runs Composer metadata validation, syntax validation, PHPUnit, the release audit, `doctor --json`, documentation generation, and deterministic `--check`.
- [x] The final integration commit has a fully green six-job OS/PHP matrix.
- [x] Composer global-install smoke and public HTTPS remote-build smoke are green on the final integration commit.
- [x] Release workflow runs the full Composer quality gate and builds a distribution archive.
- [ ] Observe the tag-triggered release workflow green for `v1.0.0`.

## Release quality and external publication

- [x] `CHANGELOG.md`, versioning policy, release process, contributor guidance, architecture/runtime-safety documentation, and example project are present.
- [x] `SourceSlate\Version::VERSION` is `1.0.0` and CLI version regression coverage exists.
- [x] Security model and source/output/cache trust boundaries are documented.
- [x] GitHub currently reports zero open SourceSlate issues; no recorded P0/P1 defect is open.
- [ ] Publish/refresh the package on Packagist and verify metadata resolves to `v1.0.0`.
- [ ] Perform clean-machine Packagist-backed global Composer installation verification.
- [ ] Perform private HTTPS and SSH credential-path verification using non-production test credentials.

## Release decision

Repository-controlled acceptance is complete for integration commit `2f29773959e8bd186ad108fa7252362468a0c9ac`.

Do **not** create the `v1.0.0` tag until the remaining external gates are complete:

1. final generated-site visual browser review;
2. private HTTPS credential-path validation;
3. private SSH credential-path validation;
4. Packagist publication/readiness and Packagist-backed clean-machine install; and
5. tag-triggered release workflow validation.

This keeps the 1.0 decision evidence-based: repository tests and CI are now complete, while external systems remain explicitly unverified until exercised.
